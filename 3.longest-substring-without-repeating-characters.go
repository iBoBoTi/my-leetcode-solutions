/*
 * @lc app=leetcode id=3 lang=golang
 *
 * [3] Longest Substring Without Repeating Characters
 */

// @lc code=start

// initialize a map
// initialize a string variable to the first character 
// loop through the string
// check at every loop if the next character is contained in the initialized string
		// if it is the map the current string with a count equal to the index plus 1
		// set the initialized string to the next character

func lengthOfLongestSubstring(s string) int {
    
}
// @lc code=end

